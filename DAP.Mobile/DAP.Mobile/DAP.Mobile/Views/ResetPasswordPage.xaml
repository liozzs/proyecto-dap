<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:customcontrols="clr-namespace:DAP.Mobile.CustomControls"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="DAP.Mobile.Views.ResetPasswordPage"
             Title="Recuperar contraseña">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Text="Email" />
        <customcontrols:RoundedEntry Grid.Row="1" Text="{Binding Email}" Keyboard="Email" MaxLength="100" />
        <Label Grid.Row="2" Text="{Binding Message}" TextColor="Red"  Margin="0, 10" />
        <StackLayout Grid.Row="4" Margin="0, 10" Orientation="Horizontal"  HorizontalOptions="Center">
            <Button Text="Cancelar" Command="{Binding CancelCommand}" WidthRequest="150" />
            <Button Text="Aceptar" Command="{Binding AcceptCommand}" WidthRequest="150" />
        </StackLayout>
    </Grid>
</ContentPage>